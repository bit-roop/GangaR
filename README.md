# GangaRakshak AI UI

Responsive Next.js UI that recreates the first screen from the provided reference image:

- Desktop landing dashboard
- Mobile landing screen

## Run locally

```bash
npm install
npm run dev
```

Then open `http://localhost:3000`.

## Files

- `app/page.tsx` contains the UI structure
- `app/globals.css` contains all styling
